spring:
  datasource:
    url: jdbc:mysql://localhost:3306/demo_db
    username: root
    password: ssh5579! 

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
